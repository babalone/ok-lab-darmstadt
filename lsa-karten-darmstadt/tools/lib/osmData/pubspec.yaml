name: osmData
description: A tool to parse OpenStreetMap-xml to a native dart-data-structure.

environment:
  sdk: '>=2.8.1 <3.0.0'

dependencies:
  xml: ^4.3.0

dev_dependencies:
  build_runner: ^1.10.0
  build_web_compilers: ^2.11.0
  pedantic: ^1.9.0
